# emacs

*.elc
*.eln
*~
#*#

auto-save-list
history
recentf
session.*

# editor / tooling caches
nano.*.save
.ruff_cache/
.virtual_documents/

# exported OCI image tars (make image-export) — large, never commit
claudecontainer-*.tar
